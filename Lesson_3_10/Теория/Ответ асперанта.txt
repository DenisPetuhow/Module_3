Приветствую Денис!

Проблема связана с тем что вы не указали путь к заголовочным файлам в настройках проекта.

Рекомендую вам в настройках проекта в разделе “С++” / “Дополнительные каталоги включаемых файлов” указывать относительный путь, например .. (это означает “каталог ниже уровнем”) или . (это означает текущий каталог). Например, вы можете написать ..\Static. Тогда все файлы .h которые находятся в директории Static будут видимы в проекте.
Кроме того, пути нужно поставить для всех конфигураций проекта.

Приветствую Денис!

$(SolutionDir)Static не работает потому что $(SolutionDir) указывает на директорию где находится решение. Ваше решение находится в директории Lesson_3_10/Static/ а следовательно $(SolutionDir)Static превращается в Lesson_3_10/Static/Static а такой директории не существует.

Ошибка “неразрешенный внешний символ” означает что ваша библиотека не линкуется к проекту.
Нужно выбрать правой кнопкой мыши решение, выбрать “зависимости сборки”, далее “зависимости проектов” и сделать так чтобы проект AnswerInformation зависел от проекта Static. Также нужно кликнуть на проекте AnswerInformation на “ссылки” и добавить ссылку на Static.
Проверьте также что вы применяете настройки ко всем конфигурациям (Debug, Release) и ко всем платформам (x64, Win32).

Есть еще другой способ - в настройках проекта AnswerInformation в “Компоновщик” - “Дополнительные каталоги библиотек” нужно указать путь к библиотеке Static.lib (это результат сборки проекта Static), а в “Ввод” - “Дополнительные зависимости” нужно указать библиотеку Static.lib. Тогда она тоже будет прилинкована к проекту.

-----------------------------------------------------ДЗ-----------------------------------------------------------
Денис, приветствую!
Есть мусор в репозитории удалите каталог с объектными файлами/экзешками во внешнем репозитории и добавьте правила в .gitignore

В других каталожках рядом тоже посмотрите.

Задание 1 - на доработку. Ошибка сборки.

1
2
Lesson_3_10\AnswerInfomation\AnswerInfomation.cpp(4,10): error C1083: Не удается открыть файл включение: Greeter.h: No such file or directory,
3>Сборка проекта "AnswerInfomation.vcxproj" завершена с ошибкой.
Вам необходимо в настройках проекта в разделе “С++” / “Дополнительные каталоги включаемых файлов” указывать относительный путь, например .. (это означает “каталог ниже уровнем”) или . (это означает текущий каталог). В этом случае, другой программист тоже сможет скомпилировать и запустить ваш код на своем компьютере, не меняя настройки проекта. Сейчас стоят такие - ;D:\Programm\2. Education\2. Netologia\1. C++\Lesson_3\1_DZ\Lesson_3_10\Static; . 
Кроме того, пути нужно поставить для всех конфигураций проекта.

Задание 2 - на доработку. Warning:

1
2
\Leaver.cpp(4,22): warning C4273: DynamicAnswer::Leaver::leave: несовместимое связывание dll
1>    Lesson_3_10\Dynamic\Leaver.h(13,16): см. предыдущее определение "leave"
У вас не совпадает макрос экспорта в коде (MATHPOWERLIBRARYDYNAMIC_EXPORTS) и в настройках проекта библиотеки (DYNAMIC_EXPORTS)

Общий механизм работы
Для таких макросов в хедере:

1
2
3
4
5
6
//Leaver.h
#ifdef LEAVER_EXPORTS  //Макрос экспорта
#define LEAVER_API __declspec(dllexport)
#else 
#define LEAVER_API __declspec(dllimport)
#endif
Макрос LEAVER_API превращается в __declspec(dllexport) или __declspec(dllimport) в зависимости от того, определен макрос LEAVER_EXPORTS или нет.

Пометкой __declspec(dllimport) помечаются функции в коде программы, которые находятся во внешней динамической библиотеке. Компоновщик видит эту отметку и ищет эту функцию не в коде программы, а в библиотеках.
Пометкой __declspec(dllexport) помечаются функции в коде библиотеки, которые будут выставлены наружу для того чтобы другие программы могли их использовать. Если вы не пометите функцию в коде библиотеки этой меткой, то функция будет “невидима” извне.

Решение для Visual Studio
Перейдите в свойства проекта динамической библиотеки. Правой кнопкой и свойства
Проверьте что наверху конфигурация: Debug, платформа: Активная x64
переходите по дереву: Свойства конфигурации->C/C++->Препроцессор
В поле “Определения препроцессора”. Перед другими макросами вида: _DEBUG;_WINDOWS;_USRDLL;%(PreprocessorDefinitions) (у вас могут немного отличаться) добавьте макрос экспорта, который прописан в хедере. Это самый главный шаг. В примере выше это будет LEAVER_EXPORTS. Итоговая строчка: LEAVER_EXPORTS;_DEBUG;_WINDOWS;_USRDLL;%(PreprocessorDefinitions)
Убедитесь, что в редакторе, в Leaver.h подсветилась синим строчка #define LEAVER_API __declspec(dllexport), при этом строчка #define LEAVER_API __declspec(dllimport)
Убедитесь, что при пересборке решения нет ворнинга C4273
Задание 3 - на доработку. Тот же warning:

1
2
3
4
5
\Leaver.cpp(4,22): warning C4273: DynamicAnswer::Leave
r::leave: несовместимое связывание dll [Lesson_3_10\Lesson_3_10_3\build\Dynamic
Lib.vcxproj]
      Lesson_3_10\Lesson_3_10_3\Leaver.h(13,15):
      см. предыдущее определение "leave"
Для CMake три варианта решения (учитывайте свои названия целей и макросов!):

Вы можете добавить макрос в настройки проекта в CMakeLists.txt. Делается это так, например: target_compile_definitions(LeaverLib PRIVATE LEAVER_EXPORTS) Более общий случай: target_compile_definitions(<ИМЯ ЦЕЛИ-БИБИОТЕКИ> PRIVATE <МАКРОС ЭКСПОРТА>)
Так как это проект CMake, то CMake в настройках проекта библиотеки автоматически добавил макрос LeaverLib_EXPORTS. Вы можете просто заменить в коде LEAVER_EXPORTS на LeaverLib_EXPORTS. В более общем случае замените свой макрос экспорта на <ИМЯ ЦЕЛИ-БИБИОТЕКИ>_EXPORTS
Нужно убрать макросы LEAVER_EXPORTS и LEAVER_API отовсюду, и просто написать в CMakeLists.txt строчку: set(CMAKE_WINDOWS_EXPORT_ALL_SYMBOLS ON) Эта команда автоматически сделает все функции и методы в библиотеке экспортируемыми.
Задание 4 - на доработку. Вот тут вы как раз столкнулись с проблемами экспорта. Из предыдущих проблем. У вас ошибка линковщика: LINK : fatal error LNK1104: не удается открыть файл "Debug\DynamicLib.lib" Происходит потому, что вы ничего не экспортируете, и собирается dll-файл без lib-файла. Он необходим для подключения. Исправьте ошибки экспорта и будет собираться/запускаться.

По заданию 5 - не совсем понял проблему. У Вас есть IDE, в ней вы удобно с помощью мыши и диалоговых окон настраиваете проект. Получается более-менее безопасно. Если вы хотите работать “пакетно”, то используйте cmake, там вы текстом всё прописываете. Вы можете отредактировать файл DynamicLib.vcxproj и вручную попытаться вставить файлы, но это опасно, чревато проблемами. Вы можете не делать этот проект, не обязателен для зачёта. Мне кажется объема работ в предыдущих 4-х хватает :)

Вы начали доработку 21 мая в 01:05
Вы загрузили решение 26 мая в 22:37

Денис Петухов
26 мая 2025 22:37
Зачем удалять каталог с объектными файлами/экзешками во внешнем репозитории??

1 Задание, тсправил путь к статической библиотеке …\static

2 Задание, у директории Dynamic добавил ->cвойства конфигурации->C/C+±>Препроцессор-> Определение предпроцессора -> изменить -> MATHPOWERLIBRARYDYNAMIC_EXPORTS . После чего #define MATHPOWERLIBRARY_API declspec(dllexport) стало синим, до этого было наоборот, что это значит?
Поясните еще раз синтаксис
#pragma once
#ifdef MATHPOWERLIBRARYDYNAMIC_EXPORTS ----За что это отвечает название???
#define MATHPOWERLIBRARY_API declspec(dllexport) — За что это отвечает ???
#else
#define MATHPOWERLIBRARY_API declspec(dllimport)
#endif**

3 Задание, добавил target_compile_definitions(DynamicLib PRIVATE MATHPOWERLIBRARYDYNAMIC_EXPORTS), это для того чтобы Cmake понимал что используем именно этот макрос экспорта?
5. Задание(файл Lesson_3_4) Добавил строку )
set(CMAKE_WINDOWS_EXPORT_ALL_SYMBOLS ON) - Эта команда автоматически сделает все функции и методы в библиотеке экспортируемыми. Или я должен по отдельности для каждого класса прописывать уникальные макросы экспорта и добавлять их через команду target_compile_definitions(DynamicLib PRIVATE MATHPOWERLIBRARYDYNAMIC_EXPORTS)?

Задание вам позднее пришлю
Вы сняли решение с проверки 26 мая в 22:44
Вы загрузили решение 26 мая в 22:45

Денис Петухов
26 мая 2025 22:45
Еще вопрос
Как зделать чтобы в репозитории корректно отображался русский язык

И задание 5 у меня собирается exe файл, по потом программа сразу завершается, необходимо паузу прописать гдето, чтобы по выполнению он сам не закрывал программу??

Антон Ласточкин отправил(a) решение на доработку 28 мая в 11:19

Антон Ласточкин
преподаватель
28 мая 2025 11:19
Добрый день.
По 1-4 заданиям зачёт, всё выполнили хорошо. Мусор еще остался.
По пятому заданию нормально посмотреть не могу - не собирается проект:

Сделайте полноценное решение, в котором будет проект с динамической библиотекой и её импортом. Посмотрим, почему не закрывается. Но вообще вы можете из проводника или консоли запустить. Это может быть следствие скрытой ошибки, она просто падает в конце.

Как зделать чтобы в репозитории корректно отображался русский язык

Кодировка в репозитории UTF-8, на винде по умолчанию Windows-1251. Если вы переделаете под UTF-8, то отображаться будет хорошо, но надо перенастраивать студию.

Кодировки файлов перевести из Cp1251 в UTF-8, это можно сделать в студии через меню сохранить как, и внизу выбираете стрелочку и сохранить с кодировкой, там ищете Юникод, UTF-8 или сторонними приложениями например через Notepad++


Настроить студию, чтобы она работала с исходниками в UTF-8 /utf-8 (Set source and execution character sets to UTF-8) | Microsoft Learn

Настройка кодировки вывода в консоль:

1
2
3
#include <Windows.h>
...
SetConsoleOutputCP(CP_UTF8);
Вывод в Visual Studio для исходников UTF-8 и Cmake

1
2
3
4
5
#Для студии ставим флаг, что кодировка исходников UTF-8
if(MSVC)
add_compile_options("$<$<C_COMPILER_ID:MSVC>:/utf-8>")
add_compile_options("$<$<CXX_COMPILER_ID:MSVC>:/utf-8>")
endif()
При работе с кодировками, отличными от ASCII, необходимо следить за ньюансами:

Кодировка исходных текстов. Для перекодировки можно пользоваться средствами текстовых редакторов таких как Notepad++ или аналогичных. Тогда компилятор сможет правильно кодировать строковые литералы в бинарное представление.
Кодировка стандартного вывода и ввода. Для Win легко настроить вывод UTF-8, во со вводом могут быть проблемы.
Кодировка чтения строк и записи в файл.
Аккуратное использование функций для строк. Например, определение длины строки UTF-8 может работать некорректно из-за того что разная длина последовательности. Лучше использовать специальные библиотеки типа легковесной UTF8-CPP: UTF-8 with C++ in a Portable Way (sourceforge.net) или более мощной unicode-org/icu: The home of the ICU project source code. (github.com)
Подробнее на примере: Обучение C++, UTF-8 с первой программы / Хабр (habr.com)